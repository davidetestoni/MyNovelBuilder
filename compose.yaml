name: mynovelbuilder

services:
  app:
    image: davidetestoni/mynovelbuilder:latest
    ports:
      - "127.0.0.1:5113:8080"
    volumes:
      - mynovelbuilder-data:/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

volumes:
  mynovelbuilder-data:
